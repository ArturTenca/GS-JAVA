# Configuração do Servidor
server.port=8080

# ============================================
# CONFIGURAÇÃO DO BANCO DE DADOS
# ============================================
# Para usar H2 (desenvolvimento): descomente as linhas abaixo
# Para usar Oracle (produção): use application-oracle.properties

# H2 Database (Desenvolvimento Local)
#spring.datasource.url=jdbc:h2:mem:upskillingdb
#spring.datasource.driverClassName=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
#spring.jpa.hibernate.ddl-auto=create-drop
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console

# Oracle Database (FIAP)
# Conexão: FIAP | Usuário: RM555171
# Hostname: oracle.fiap.com.br | Port: 1521 | SID: ORCL
spring.datasource.url=jdbc:oracle:thin:@oracle.fiap.com.br:1521:ORCL
spring.datasource.username=RM555171
spring.datasource.password=261205
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Configuração JPA/Hibernate
# IMPORTANTE: Como as tabelas já foram criadas pelo script SQL, use 'validate' ou 'none'
# 'validate' - apenas valida o schema, não modifica
# 'none' - não faz nada com o schema
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuração de Logging
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Configuração de Mensagens de Validação
spring.messages.basename=messages
spring.messages.encoding=UTF-8

